# ascii-cpumap

Per-core ASCII CPU heatmap for the terminal.

## Features

- Shows CPU usage per core
- Draws compact ASCII load cells
- Refreshes live
- Pure Python stdlib
- Reads data from `/proc/stat`

## Usage

```bash
python3 main.py
```

Press `q` to quit.

## Notes

Designed for Linux terminals.
